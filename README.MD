
# 🍏 iStore - Apple Shop Concept

**iStore** — це легкий та швидкий веб-додаток для перегляду та пошуку продуктів Apple. Проект демонструє сучасний підхід до розробки на "чистому" JavaScript (Vanilla JS) з використанням модульної архітектури та компонентного підходу в CSS.

---

## 🚀 Функціонал

* **Динамічний рендеринг:** Список товарів генерується автоматично на основі масиву даних.
* **Миттєвий пошук:** Фільтрація товарів у реальному часі за назвою або категорією (наприклад, "iPhone" або "Smartphone").
* **Адаптивний дизайн:** Магазин коректно відображається на смартфонах, планшетах та ПК завдяки Flexbox.
* **Інтерактивність:** Анімації при наведенні на картки та функціональна кнопка "Купити".

---

## 🛠 Технологічний стек

* **HTML5:** Семантична розмітка сторінки.
* **SCSS/CSS:** Модульна структура стилів з використанням змінних та вкладеності.
* **JavaScript (ES6+):** Модульна система (Import/Export), маніпуляції з DOM.

---

## 📂 Структура проекту

Проект організований за принципом розділення відповідальності (Separation of Concerns):

```text
├── index.html          # Головна точка входу
├── js/
│   ├── main.js         # Запуск додатку
│   ├── app/
│   │   └── initApp.js  # Ініціалізація логіки та обробників
│   ├── data/
│   │   └── products.js # База даних товарів (JSON-подібний масив)
│   ├── logic/
│   │   └── filterProducts.js # Алгоритм пошуку
│   ├── ui/
│   │   └── renderProducts.js # Генерація HTML-карток
│   └── handlers/
│       └── onSearch.js # Обробка подій введення
└── scss/               # Стилі, розбиті за компонентами

```

---

## ⚙️ Як запустити

1. **Клонуйте репозиторій:**
```bash
git clone https://github.com/ваш-логін/istore-apple-shop.git

```


2. **Відкрийте проект:**
Просто відкрийте файл `index.html` у вашому браузері.
*Примітка: Оскільки використовуються JS-модулі, рекомендується використовувати локальний сервер (наприклад, розширення "Live Server" у VS Code).*

---

## 💡 Приклад коду логіки пошуку

Для фільтрації ми використовуємо метод `.filter()`, який перевіряє збіги незалежно від регістру:

```javascript
export const filterProducts = (products, searchTerm) => {
    const term = searchTerm.toLowerCase().trim();
    if (!term) return products;

    return products.filter(
        (product) =>
            product.title.toLowerCase().includes(term) ||
            product.type.toLowerCase().includes(term)
    );
};

```

---

## 📈 Плани на майбутнє

* [ ] Додати сторінку "Кошик".
* [ ] Реалізувати детальну сторінку товару.
* [ ] Підключити зовнішній API для отримання актуальних цін.
* [ ] Додати темну тему.

---

**Розроблено з ❤️ для фанатів Apple та чистого коду.**
